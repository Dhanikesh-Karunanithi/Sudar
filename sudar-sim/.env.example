# SudarSim voice service
SIM_DEV_MODE=1
PORT=8090
SUDAR_INTELLIGENCE_URL=http://localhost:8001
INTELLIGENCE_SERVICE_SECRET=

# Supabase (session validation, optional direct reads)
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# LiveKit (production — unset SIM_DEV_MODE when using LiveKit)
LIVEKIT_URL=
LIVEKIT_API_KEY=
LIVEKIT_API_SECRET=

# STT / TTS (optional for dev — uses Intelligence text-only fallback)
DEEPGRAM_API_KEY=
CARTESIA_API_KEY=
