/**
 * Sudar Studio — platform knowledge for product agents (menu paths, ALP summary, etc.).
 * Source of truth: help-center/_ai/studio-navigation.md
 * Regenerate TS: `npm run help-center:sync-ai` from repo root (also runs automatically before Sudar Studio `npm run build`).
 * Sudar Agents architecture: docs/AGENTS_PLATFORM.md
 */
export { SUDAR_STUDIO_PLATFORM_KNOWLEDGE } from './platformKnowledge.generated'
