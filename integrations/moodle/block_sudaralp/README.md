# block_sudaralp

Moodle block that links to Sudar **next action** and **tutor** pages from the Dashboard or a course. Requires [`local_sudaralp`](../local_sudaralp/README.md) to be installed and configured.

Install: copy this folder to `moodle/blocks/sudaralp`, then upgrade the site.
