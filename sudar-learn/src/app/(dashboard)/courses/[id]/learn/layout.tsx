import { CourseLearnShell } from './CourseLearnShell'

export default function CourseLearnLayout({ children }: { children: React.ReactNode }) {
  return <CourseLearnShell>{children}</CourseLearnShell>
}
